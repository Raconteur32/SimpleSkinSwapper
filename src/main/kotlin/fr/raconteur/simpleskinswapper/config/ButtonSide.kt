package fr.raconteur.simpleskinswapper.config

/**
 * Side of the vanilla bottom button row on which the skin-preview menu button
 * is injected. Serialized by Gson as the constant name ("LEFT"/"RIGHT").
 */
enum class ButtonSide {
    LEFT,
    RIGHT
}
