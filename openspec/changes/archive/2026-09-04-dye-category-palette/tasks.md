## Tasks

- [x] 1. Spike the icon render path: blit a dye atlas sprite through the GUI pipeline in all 4 generated trees (preferred: `blitSprite` sprite overload; fallback: atlas bind + `getU0()/getV0()`); resolve the 16 dye map colors/ARGBs; record the chosen path and values in design.md; commit
      RESULT: blitSprite(TextureAtlasSprite) overload exists in all 4 trees; only acquisition branches (1.21.11 blocks atlas via Material, 26.x items atlas via SpriteId/AtlasIds). Palette derives from DyeColor at runtime. LESSON: src/main/kotlin is chiseled for vcs version 26.2 — new branched files must be written pre-commented (raw directives break the active compile).
- [x] 2. Rework `SkinCategoryPalette` to the 16 dyes (ENTRIES from DyeColor wool values, `swatches()` → dye list, DEFAULT_HEX → blue dye); keep `toHex`/`parse` contracts; all existing consumers build and detekt green; commit
- [x] 3. Band picker: 16 dye icons in 8×2 (replacing the 10×2 `fill()` grid), white border on selected dye, no highlight when no dye matches, vanilla dye-name tooltip; re-run the band layout_check script if geometry shifts; commit
      RESULT: grid now 8×2, same 12px cells; geometry consumers (nameField x, stepper label) updated consistently. layout_check script no longer exists in repo (reference only). NOTE: the new per-version gate caught its first real divergence — render/extractRenderState hits complexity 15 only in the 1.21.11 flavor; fixed by extracting drawEmptyStateMessage.
- [x] 4. Category tab: dye icon (8×8, grow if illegible in game) replacing the flat color square; test in game on 26.3 (picker, tab, wheel sectors, card tints — wool palette look); commit
      RESULT: 8×8 dye icon on the tab; legacy colors (no dye match) keep the flat square. Build + detektAll green. PENDING: user in-game test on 26.3 for the muted wool look and icon legibility.
