## Tasks

- [x] 1. Tab strip: draw the add-category entry (strip sprite, `#999999` outline, centered "+", hover brighten) after the last tab, scrolling with the strip; extend the strip hit-test for it; wire click to create-select-open-band with the incremented default name; remove the vanilla "+" button; commit
- [x] 2. Footer: lift `gridBottom` to reserve the footer band; spread the three 110px buttons across the panel width (left/center/right); commit
- [x] 3. Lang keys: restore `simpleskinswapper.screen.library.add_category` as "New Category" / "Nouvelle catégorie"; increment logic at the creation call site (landed with task 1 via nextDefaultCategoryName); `./gradlew build detektAll` green; commit
- [x] 4. In-game test on 26.3 (entry look + hover, creation flow, increment, footer layout, GUI scale auto); commit any polish fallout
      FALLBACK ROUND 1 (user-tested): scroll max is now a whole-tab multiple (no half-hidden tab at the limit) and stripBottom tracks gridBottom (FOOTER_BAND had desynced them); footer buttons spread across the FULL screen width; wheel count now drawn between [-] and [+] (missed from the original capture — restored); DEFAULT_HEX derives from the white dye entry (was off-palette by derivation, drew the fallback square on the tab).
      FALLBACK ROUND 2 (user-tested): the half-hidden tab at the strip BOTTOM was the clip window itself — strip height isn't a whole-tab multiple, so a 20px sliver of the next tab peeked below. New stripAlignedBottom() = stripTop + wholeTabs*TAB_H: all tab scissors, hit-tests (tabAt/addEntryAt), the add entry and the insertion line clip to it; the book-panel frame still spans the full grid height (dead zone = breathing room, matches the user's observation that space opened under the strip).
      FALLBACK ROUND 3 (user request): tab height itself is now dynamic — recomputeLayout splits the strip into whole tabs of at least 28px (tabH = stripHeight / visibleTabs), so the strip fills edge-to-edge and the leftover shrinks to a few px instead of a visible margin. Selected/dragged tab textures follow automatically (the book-panel sprite blits stretched to tabH). Controller reads the height via a tabH lambda.
      FALLBACK ROUND 4 (user-tested): removed the legacy tab hover overlay (didn't match real tab placement anymore); add-category frame inset by 1px on all sides.
      FALLBACK ROUND 5 (user-tested): unselected tabs each wear their own unlit book panel — same shape as the selected tab but flush with the strip's right edge (no page tuck) — so panel borders read as separators; the add-category entry renders as a pseudo-tab (own panel + centered "+", glyph brightens on hover) and the outline frame is gone. The big strip background stays as the under-layer for corner transparency.
      FALLBACK ROUND 6 (user-tested): tabs now overlap by 2px (slotH = tabH - 2) — each panel's top border covers the one above, drawn top-to-bottom so lower panels win; the selected and dragged tabs stay in the over pass, always on top. Hit-test scans bottom-up to match the draw order; scroll steps, insertion gap and the insertion line all move in slotH units; maxTabScroll rounds the overflow up to whole slots.
      FALLBACK ROUND 7 (user-tested): unselected panels now scissor from -PANEL_BLEED so they reach the screen's left edge like the selected one (the STRIP_X bound left a gray margin); tab height is now computed from the CURRENT slot count — tabH = (H + overlap*(slots-1))/slots clamped to [28,48] — so the strip fills edge-to-edge again despite the 2px overlaps (the old count-blind formula under-filled by 2px per tab).
      FALLBACK ROUND 8 (user-tested): the aligned-bottom clip was still tabH-quantized (floor(H/tabH)*tabH), wasting up to 47px with dynamic tabH and re-breaking the scroll. It now quantizes to whole SLOTS — k = floor((H-tabH)/slotH)+1 full tabs, clip = (k-1)*slotH+tabH — which in the unclamped regime lands exactly on the content end (no margin, no scroll) and in the clamped regime clips sliver-free with max scroll rounded up to whole slots.
      FALLBACK ROUND 9 (user-tested): the 48px upper clamp was the gray step — with few categories contentH couldn't reach H, leaving exactly one slot of bare background. Removed the clamp: tabH = max(28, (H + overlap*(slots-1))/slots) — few categories grow taller tabs and always fill the strip edge-to-edge; the 28px floor only matters once the list is long enough to scroll.
      FALLBACK ROUND 10 (user-tested + IN-GAME RUN): ran the client with SSS_DEBUG_LIBRARY auto-open + [StripDebug] layout logs. User has 10 categories (slots=12) — scrolled regime, and the aligned clip was STILL quantized by the old min-28 density: 24px of background at the bottom at every scroll position. Fix: tab height is now derived from a ~28px DENSITY in both regimes — k = round(H/28), tabH = (H + overlap*(k-1))/k, or k = slot count when the list is shorter than the strip. Verified in-game log: tabH=27 slotH=25, clip flush at 203, residual background 5px (hairline), scroll aligned at every step, no half tabs.
      FALLBACK ROUND 11 (user-described misalignment, in-game verified): the strip now anchors on the CARD PAGE's vertical span (gridTop-PAGE_BORDER .. gridBottom+PAGE_BORDER) instead of the bare grid — the tab band tiles that zone with whole slots and centers the rounding remainder (log: zone 18..246, band 19..245, tabH=30 x8, 1px inset each side), the strip background spans the full zone, and the clamp/scroll/hit-tests all read the band. No more 4-5px step at the bottom.
      FALLBACK ROUND 12 (user-tested): the band must stay RECESSED — zone reverted to the strip's own footprint [gridTop, gridBottom] (8px inside the card page), band centered within, background back on that footprint. In-game log: zone 26..238 vs page 18..246, band 27..237, tabH=28 x8, waste 2px split 1/1, clamp flush with the band.
      FALLBACK ROUND 13 (user request): removed the zone-wide strip background entirely — the per-tab panels are the only dressing now; recessed margins around the band show the screen backdrop.
      FALLBACK ROUND 14 (user-tested): whole-tab containment at the band's bottom edge — draw loop, add-entry visibility and hit-tests now skip any tab whose bottom crosses the aligned clip, so the next tab's 2px overlap border no longer dangles past the last full tab (which keeps its full bottom border).
      FALLBACK ROUND 15 (user-tested): wheel scroll stepped tabH while slots advance slotH — every notch drifted the stack 2px off its grid. Wheel now steps slotH; in-game log confirms maxScroll is an exact slot multiple.
      FALLBACK ROUND 16 (user-tested): mirrored the whole-tab containment at the band's TOP edge for the over pass — a selected tab scrolled half out no longer dangles its overlap border over the first visible tab; hit-tests (tabAt/addEntryAt) skip partially-visible tabs at the top too.
      FALLBACK ROUND 17 (user-reported): adding a skin from a selected category now files it into that category (assignSkin appends; All skins keeps it visible as member). NOTE: this INVERTED a spec mandate ("not silently assigned", scenario "Import lands in All skins") — the change delta now MODIFIES the add-flow requirement accordingly (All skins adds stay unassigned).
      FALLBACK ROUND 18 (user-tested, in-game [DragDebug] trace): in-category reorder PERSISTS but the display never refreshed — finishCardReorder's grid-drop branch mutated+saved category.skins then rebuilt from stale `entries` (rebuildCards iterates entries; the tab-drop branch called reloadView, the grid branch never did). Fix: reloadView() before rebuildCards(). [DragDebug] logs left in for the user's next verification round.
- [x] 5. Tab names too long for the tab render truncated with an ellipsis instead of hard mid-glyph clipping (tooltip already shows the full name on hover); commit
